# API Service

Future `FastAPI` service for:
- team reads
- match reads
- metric endpoints
- tactical report endpoints

Keep analytics logic out of request handlers. Heavy computation should live in `jobs/etl`.

