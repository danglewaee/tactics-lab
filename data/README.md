# Data Notes

Use this directory for local datasets during development.

- `raw/`: untouched provider payloads
- `processed/`: normalized extracts or cached feature tables
- `manual/`: small curated context files, such as player position frequency

Keep large files out of version control.
